{
  "name": "invoxira-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm run install-backend && cd frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm ci --include=dev && npm run build",
    "install-backend": "cd backend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.6.3"
  }
}
